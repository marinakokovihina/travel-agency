import React from 'react';
import {StyledBody, StyledMain} from "./StyledAuth";

const LoginPage = () => {
    return (
        <StyledBody>

        </StyledBody>
    );
};

export default LoginPage;
