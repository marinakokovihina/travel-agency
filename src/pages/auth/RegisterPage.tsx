import React from 'react';
import {StyledBody} from "./StyledAuth";

const RegisterPage = () => {
    return (
        <StyledBody>
            REGISTER
        </StyledBody>
    );
};

export default RegisterPage;
